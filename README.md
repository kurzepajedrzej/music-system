# music-system

Merged Python backend (FastAPI) and SvelteKit frontend for the home music
system — replaces `music-backend`, `cd-control`, and `music-frontend`.
See `backend/README.md` and `frontend/README.md` for each half, and
`homelab-configs/CLAUDE.md` for how this fits into the wider server.
